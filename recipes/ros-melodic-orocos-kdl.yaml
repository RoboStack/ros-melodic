package:
  name: ros-melodic-orocos-kdl
  version: 1.4.0
source:
  git_url: https://github.com/orocos/orocos-kdl-release.git
  git_rev: release/melodic/orocos_kdl/1.4.0-0
  folder: ros-melodic-orocos-kdl/src/work

build:
  script:
    sel(win): bld_catkin.bat
    sel(unix): build_catkin.sh
  number: 2
about:
  home: https://www.ros.org/
  license: BSD-3-Clause
  summary: |
    Robot Operating System

extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - "{{ compiler('cxx') }}"
    - "{{ compiler('c') }}"
    - ninja
    - sel(unix): make
    - cmake
  host:
    - cmake
    - cppunit
    - eigen
    - pkg-config
    - python
    - ros-melodic-catkin
  run:
    - eigen
    - pkg-config
    - python
    - ros-melodic-catkin
