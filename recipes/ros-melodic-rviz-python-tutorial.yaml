package:
  name: ros-melodic-rviz-python-tutorial
  version: 0.10.5
source:
  git_url: https://github.com/ros-gbp/visualization_tutorials-release.git
  git_rev: release/melodic/rviz_python_tutorial/0.10.5-1
  folder: ros-melodic-rviz-python-tutorial/src/work

build:
  script:
    sel(win): bld_catkin.bat
    sel(unix): build_catkin.sh
  number: 2
about:
  home: https://www.ros.org/
  license: BSD-3-Clause
  summary: |
    Robot Operating System

extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - "{{ compiler('cxx') }}"
    - "{{ compiler('c') }}"
    - ninja
    - sel(unix): make
    - cmake
  host:
    - python
    - ros-melodic-catkin
    - ros-melodic-rviz
  run:
    - python
    - ros-melodic-rviz
