package:
  name: ros-melodic-diagnostic-updater
  version: 1.9.7
source:
  git_url: https://github.com/ros-gbp/diagnostics-release.git
  git_rev: release/melodic/diagnostic_updater/1.9.7-1
  folder: ros-melodic-diagnostic-updater/src/work

build:
  script:
    sel(win): bld_catkin.bat
    sel(unix): build_catkin.sh
  number: 2
about:
  home: https://www.ros.org/
  license: BSD-3-Clause
  summary: |
    Robot Operating System

extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - "{{ compiler('cxx') }}"
    - "{{ compiler('c') }}"
    - ninja
    - sel(unix): make
    - cmake
  host:
    - python
    - ros-melodic-catkin
    - ros-melodic-diagnostic-msgs
    - ros-melodic-roscpp
    - ros-melodic-rostest
    - ros-melodic-std-msgs
  run:
    - python
    - ros-melodic-diagnostic-msgs
    - ros-melodic-roscpp
    - ros-melodic-std-msgs
