package:
  name: ros-melodic-tf2-py
  version: 0.6.5
source:
  git_url: https://github.com/ros-gbp/geometry2-release.git
  git_rev: release/melodic/tf2_py/0.6.5-0
  folder: ros-melodic-tf2-py/src/work
  patches:
    - patch/ros-melodic-tf2-py.patch

build:
  script:
    sel(win): bld_catkin.bat
    sel(unix): build_catkin.sh
  number: 2
about:
  home: https://www.ros.org/
  license: BSD-3-Clause
  summary: |
    Robot Operating System

extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - "{{ compiler('cxx') }}"
    - "{{ compiler('c') }}"
    - ninja
    - sel(unix): make
    - cmake
  host:
    - python
    - ros-melodic-catkin
    - ros-melodic-rospy
    - ros-melodic-tf2
  run:
    - python
    - ros-melodic-rospy
    - ros-melodic-tf2
